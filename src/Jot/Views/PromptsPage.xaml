<Page x:Class="Jot.Views.PromptsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:models="clr-namespace:Jot.Models"
      Title="Prompts">
    <Grid Margin="{StaticResource PagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Prompts" Style="{DynamicResource TitleTextBlockStyle}" />
            <TextBlock Text="Ready-made and custom instructions for Rewrite. Pin the ones you use most."
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <ui:TextBox Grid.Row="1" Margin="0,0,0,10" PlaceholderText="Search prompts"
                    Icon="{ui:SymbolIcon Search24}"
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

        <ListView Grid.Row="2" ItemsSource="{Binding Prompts}" Background="Transparent" BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  VirtualizingPanel.IsVirtualizingWhenGrouping="True">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Margin" Value="0,0,0,6" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" Margin="2,12,0,6" FontWeight="SemiBold"
                                       Style="{DynamicResource CaptionTextBlockStyle}"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type models:PromptItem}">
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                            CornerRadius="6" Padding="14,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Body}" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" />
                            </StackPanel>
                            <ui:Button Grid.Column="1" Appearance="Transparent" VerticalAlignment="Center"
                                       Command="{Binding DataContext.TogglePinCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                       CommandParameter="{Binding}" ToolTip="Pin to picker">
                                <Grid>
                                    <ui:SymbolIcon Symbol="Pin24"
                                                   Visibility="{Binding IsPinned, Converter={StaticResource BoolToVis}, ConverterParameter=invert}" />
                                    <ui:SymbolIcon Symbol="Pin24" Filled="True" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"
                                                   Visibility="{Binding IsPinned, Converter={StaticResource BoolToVis}}" />
                                </Grid>
                            </ui:Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Add a custom prompt -->
        <ui:CardExpander Grid.Row="3" Margin="0,10,0,0" Icon="{ui:SymbolIcon Add24}">
            <ui:CardExpander.Header>
                <TextBlock Text="Add your own prompt" FontWeight="SemiBold" />
            </ui:CardExpander.Header>
            <StackPanel>
                <ui:TextBox PlaceholderText="Title" Margin="0,0,0,8"
                            Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}" />
                <ui:TextBox PlaceholderText="Instruction (e.g. Rewrite this as a friendly tweet)"
                            AcceptsReturn="True" TextWrapping="Wrap" MinHeight="60"
                            Text="{Binding NewBody, UpdateSourceTrigger=PropertyChanged}" />
                <ui:Button Content="Add prompt" Appearance="Primary" Margin="0,10,0,0"
                           HorizontalAlignment="Right" Command="{Binding AddPromptCommand}" />
            </StackPanel>
        </ui:CardExpander>
    </Grid>
</Page>
