<Page x:Class="Jot.Views.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="About">
    <Grid Margin="{StaticResource PagePadding}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="420">
            <ui:SymbolIcon Symbol="Info24" FontSize="40" HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            <TextBlock Text="About Jot" Style="{DynamicResource TitleTextBlockStyle}"
                       HorizontalAlignment="Center" Margin="0,12,0,4" />
            <TextBlock Text="Free, on-device dictation for Windows. Nothing you say leaves your PC."
                       Style="{DynamicResource BodyTextBlockStyle}"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       TextAlignment="Center" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Page>
