<Page x:Class="Jot.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Settings">
    <Grid Margin="{StaticResource PagePadding}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="420">
            <ui:SymbolIcon Symbol="Settings24" FontSize="40" HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            <TextBlock Text="Settings" Style="{DynamicResource TitleTextBlockStyle}"
                       HorizontalAlignment="Center" Margin="0,12,0,4" />
            <TextBlock Text="General, Transcription, AI, Prompts, Vocabulary, Sound, and Shortcuts."
                       Style="{DynamicResource BodyTextBlockStyle}"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       TextAlignment="Center" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Page>
