<Page x:Class="Jot.Views.ShortcutsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:ctl="clr-namespace:Jot.Controls"
      Title="Shortcuts">
    <Page.Resources>
        <Style x:Key="SectionHeader" TargetType="TextBlock" BasedOn="{StaticResource SubtitleTextBlockStyle}">
            <Setter Property="Margin" Value="2,22,0,8" />
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="{StaticResource PagePadding}">
        <StackPanel MaxWidth="820">
            <TextBlock Text="Shortcuts" Style="{DynamicResource TitleTextBlockStyle}" Margin="0,0,0,4" />

            <!-- Editable rows are click-to-capture HotkeyBoxes (TwoWay-bound; App re-registers on the
                 settings-changed signal). Stop & save is fixed to Esc, shown read-only: a HotkeyBox
                 can never capture Esc (Esc cancels the capture) and Esc is the natural stop key. -->
            <ui:InfoBar IsOpen="True" Severity="Informational" IsClosable="False" Margin="0,12,0,4"
                        Title="Change a shortcut"
                        Message="Click a shortcut, then press the new key combination. Backspace clears it; Esc cancels the capture." />

            <ctl:SettingRow Title="Toggle recording" Description="Start and stop dictation.">
                <ctl:HotkeyBox Chord="{Binding ToggleRecordingHotkey, Mode=TwoWay}" />
            </ctl:SettingRow>
            <ctl:SettingRow Title="Rewrite selection"
                            Description="Rewrite the highlighted text with AI, using your default prompt (or pick one).">
                <ctl:HotkeyBox Chord="{Binding RewriteHotkey, Mode=TwoWay}" />
            </ctl:SettingRow>
            <ctl:SettingRow Title="Paste last transcript"
                            Description="Paste your most recent transcript (AI-rewritten or raw) at the cursor from anywhere.">
                <ctl:HotkeyBox Chord="{Binding PasteLastHotkey, Mode=TwoWay}" />
            </ctl:SettingRow>
            <ctl:SettingRow Title="Stop &amp; save recording"
                            Description="Ends the current recording and saves it (active only while recording). Fixed to Esc.">
                <Border Padding="12,6" CornerRadius="4" MinWidth="72" HorizontalAlignment="Left"
                        Background="{DynamicResource ControlFillColorDefaultBrush}"
                        BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
                    <TextBlock Text="Esc" HorizontalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </Border>
            </ctl:SettingRow>

            <Border Height="24" />
        </StackPanel>
    </ScrollViewer>
</Page>
