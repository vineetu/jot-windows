<UserControl x:Class="Jot.Controls.SettingRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1" CornerRadius="6" Padding="16,12" Margin="0,0,0,4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                <TextBlock Text="{Binding Title, ElementName=Root}" TextWrapping="Wrap" />
                <TextBlock Text="{Binding Description, ElementName=Root}" Margin="0,2,0,0"
                           FontSize="12" TextWrapping="Wrap"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Visibility="{Binding Description, ElementName=Root, Converter={StaticResource StringToVis}}" />
            </StackPanel>
            <ContentPresenter Grid.Column="1" Content="{Binding RowContent, ElementName=Root}"
                              VerticalAlignment="Center" />
        </Grid>
    </Border>
</UserControl>
