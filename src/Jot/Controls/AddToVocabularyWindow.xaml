<ui:FluentWindow x:Class="Jot.Controls.AddToVocabularyWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Add to Vocabulary"
                 Width="420" SizeToContent="Height" ResizeMode="NoResize"
                 WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
                 ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica" WindowCornerPreference="Round">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Add to Vocabulary" />

        <StackPanel Grid.Row="1" Margin="24,8,24,20">
            <TextBlock Text="When Jot hears" Margin="0,0,0,4" />
            <ui:TextBox x:Name="HeardBox" FontFamily="{StaticResource JotMonoFont}" />

            <TextBlock Text="Spell it as" Margin="0,14,0,4" />
            <ui:TextBox x:Name="TermBox" FontFamily="{StaticResource JotMonoFont}" />

            <TextBlock x:Name="StatusText" Margin="0,14,0,0" TextWrapping="Wrap" FontSize="12"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <ui:Button Content="Cancel" Margin="0,0,8,0" Click="OnCancel" IsCancel="True" />
                <ui:Button x:Name="AddButton" Content="Add" Appearance="Primary" Click="OnAdd" IsDefault="True" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
