<Project Sdk="Microsoft.NET.Sdk">

  <!-- Deliberately NOT referenced by Jot and not in any solution: this is a bench instrument, never
       shipped. Build/run it explicitly (see README.md in this folder). -->
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>EdrSim</AssemblyName>
    <RootNamespace>EdrSim</RootNamespace>
    <PlatformTarget>x64</PlatformTarget>
  </PropertyGroup>

</Project>
